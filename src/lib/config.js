const config = {
  backendApiUrl: import.meta.env.VITE_BACKEND_API_URL,
  appendTenentId: import.meta.env.VITE_APPEND_TENENT_ID,
  tenentId: import.meta.env.VITE_TENEND_ID,
};

export default config;
